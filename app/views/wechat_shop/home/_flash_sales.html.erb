<div class="home-section flash-sales" onclick="App.showProductDetail(this);" data-id="<%= @product.id %>" data-sale-id="<%= @sale.id %>">
  <div class="row">
    <div class="col-xs-5">
      <div class="sale-info">
        <h2 class="caption red">限时抢购</h2>
        <p class="time-tip">
          <% if @start > 0 %>
          距离开始时间
          <% elsif @end > 0 %>
          距离结束时间
          <% else %>
          已结束
          <% end %>
        </p>
        <p class="countdown" data-duration="<%= @duration %>">
          <span class="digit hour"><%= @hour %></span><span class="dot">:</span><span class="digit min"><%= @min %></span><span class="dot">:</span><span class="digit sec"><%= @sec %></span>
        </p>
        <p><span class="red price">¥ <%= @product.price %></span><span class="m-price">¥ <%= @product.m_price %></span></p>
      </div>
    </div>
    <div class="col-xs-7">
      <div class="product-image">
        <%= image_tag @product.images.first.url(:cover_image), class: 'img-responsive' %>
      </div>
    </div>
  </div>
</div>